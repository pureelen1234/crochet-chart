# 코바늘 도안 만들기 — 작업 맥락

이 파일은 다른 컴퓨터/새 세션에서 작업을 이어갈 때 읽는 문서입니다. 코드 구조는 README.md, 여기는 "왜 이렇게 만들었는지"와 진행 상황.

## 프로젝트 목적
- 사용자(남편)가 만들고, **아내(코바늘 뜨개 하시는 분)가 아이패드 미니 7(주 기기)·아이폰 Safari에서 홈 화면 앱으로 사용**하는 도안 생성기.
- **진짜 용도(2026-09-19 확인)**: 아내가 실력이 늘어 즉흥으로 뜨는 게 많아졌고, 그걸 잊기 전에 기록하려고 손으로 그리던 것을 대신함. 남의 도안 옮겨 적기가 아니라 **본인 패턴 기록**이 핵심.
  - 기록 시점: 뜨는 도중이 아니라 **한 단/모티브 끝내고 한꺼번에**. (실시간 코 카운터는 필요 없음 — 아내가 명시적으로 거절)
  - 아이패드는 가로·세로 비슷하게 씀 → 두 레이아웃 모두 필요. **키보드가 없는 상황이 기본** → 키패드로 입력.
  - 키패드 버튼은 **기호 크게 + 이름 작게** 둘 다. 주로 쓰는 기호: 사슬, 짧은뜨기, 긴뜨기, 한길긴뜨기, 두길긴뜨기, 빼뜨기.
- 남편분은 뜨개 지식이 없음. 뜨개 관련 판단은 **아내 피드백이 기준**. 피드백은 남편이 카톡 내용을 그대로 전달해 줌.
- 참고 서비스: 패터너스 patterners.com (손으로 기호를 놓고 ＋핸들을 끌어 반복 배치하는 에디터). 우리는 텍스트(=키패드) 입력 → 자동 배치 방식을 유지하기로 함.

## 배포
- GitHub Pages: https://pureelen1234.github.io/crochet-chart/ (저장소 pureelen1234/crochet-chart, 공개)
- `git push origin main` 하면 1~2분 뒤 반영. 빌드 없음(정적 파일).
- 파일 바꿀 때 `sw.js`의 `CACHE` 버전을 올려야 아이폰의 오프라인 캐시가 교체됨 (현재 v7).
- 로컬 미리보기: 윈도우 `powershell -ExecutionPolicy Bypass -File serve.ps1`, 맥 `node serve.mjs` → http://localhost:8765 (Claude 브라우저 패널은 `.claude/launch.json`의 `preview-mac`)
- 여러 컴퓨터에서 작업: 시작할 때 `git pull`, 끝날 때 `git push`. push까지 해야 배포됨.
- 커밋 시 git 사용자: `git -c user.name="pure_" -c user.email="pureelen2@gmail.com" commit …` (전역 설정 없음)

## 결정된 사양 (아내 확인 완료)
1. 코 나열은 **반시계방향**, 매 단 **12시에서 출발**. (평면은 홀수단 ←, 짝수단 →)
2. 규격 3종: 원형 / 왕복(평면) / 모티브(사각). 사각은 가운데 매직링에서 네 모서리로 퍼지는 방식.
3. `매직링` 시작 → 원 하나(○). 1단 코 수 자유.
4. `기둥사슬 N` = 단 시작 사슬, 세로로 쌓아 그림. `기둥사슬 1` = 0코, 2 이상 = 1코(전 단 첫 코 대신 → 전 단 코 1개를 소모).
5. 사슬은 이어진 묶음(다발)으로 그림. 사각 모서리(사슬 2 이상) = ^ 모양, 변 사이 = 눕힌 모양.
6. `사슬2 공간에 (…)` / `사슬1 공간에 한길긴뜨기 3` = 전 단의 사슬 공간에 뜸. 아내가 평소 쓰는 표현이 "사슬2 공간에".
7. 단 끝 `빼뜨기` = 이음(●), 코 수 제외.
8. 그래니 스퀘어(4단)가 사각 규격 테스트 도안. 사진 도안과 비슷하게 나오는 것이 목표.

### 내가 정한 가정 (아내 확인 안 됨 — 피드백 오면 바꿀 것)
- 단이 `공간에`로 시작하면 전 단의 **첫 번째** 사슬 공간(반시계 순서상 첫 모서리)에서 시작. (원래 "마지막 공간"으로 하기로 했으나, 참고 도안에서 이음·기둥사슬이 모두 12시 근처에 모이는 걸 보고 바꿈. `layout.js` `assign()`의 `spaces` 순서 한 줄.)
- 단 번호 라벨은 도안 왼쪽 위에 "N단 M코" 목록으로.
- **기호는 머리(가로선) 기준으로 배치** (6차): 머리가 단 선(둘레) 위에 오고 기둥은 안쪽으로 뻗음. 같은 공간 묶음은 머리 높이가 같고 기둥만 앵커로 모임(`headToCenter`, `halfLen`). 기둥사슬은 타원 N개를 기둥 길이(`len`)만큼 늘여 그림. 아내 사진 도안 기준.
- **머리는 둘레에 고르게 펼침** (7차, `spreadHeads`): 앵커 기반 배치 뒤, 이웃 묶음 중심 사이 구간을 [앞 묶음 절반 + 사이 사슬 + 뒤 묶음 절반] 폭 가중치(한길긴뜨기·기둥사슬 1, 사슬 0.8/개)로 균등 분배. 묶음은 앵커 중심 대칭, 간격은 `FAN`(최소)~`2*S`(최대). 기둥 방향(앵커)은 그대로라 부채가 넓어짐. 원형·사각 공통, 평면 제외.
- 같은 공간에 모여 뜨는 묶음의 기호 끝 간격 `FAN = 9.5`(최소값으로만 쓰임), 일반 코 간격 `S = 11`, 단 높이 `rowGap()` = 2×(가장 큰 기호 반높이)+16, 26~40 사이. 선 굵기 `INK_W = 1.1`(app.js).

## 입력 방식 (5차부터)
- 텍스트가 여전히 원본. 키패드(`app.js` "코 키패드" 블록)는 **현재 줄(curLine)** 끝에 낱말을 이어 붙이며 쉼표·띄어쓰기를 자동으로 넣는 앞단.
  - 규칙: 기호 뒤 기호 → ", " / 숫자 뒤 숫자 → 이어 붙임(두 자리) / `)` 뒤 숫자 → `*N` / 숫자 뒤 늘려·모아뜨기 → "N코 늘려뜨기" / `공간에` 뒤 → 띄어쓰기만
  - 단 칩을 누르면 그 줄로 이동, `↵ 다음 단`이 새 줄, `⌫`는 낱말 하나 삭제, `↶`는 키패드 조작 되돌리기
- 터치 기기에서는 입력칸 `inputmode="none"`이라 소프트 키보드가 안 뜸. "⌨ 키보드로 입력" 버튼으로 전환(저장 안 함, 매번 기기 기준).
- 파서: `사슬2 공간에 (…)*4`는 반복마다 **다른** 공간(4곳)에 뜨는 것으로 해석 (5차에서 바꿈).
- 레이아웃: 900px 미만(휴대폰·아이패드 세로) = 도안 → 입력 → 키패드 세로 순서, 도안 높이 36vh라 아이패드 미니 세로에 세 개가 한 화면. 900px 이상(아이패드 가로·PC) = 왼쪽 입력+키패드, 오른쪽 도안.

## 구조 요약
- `symbols.js` 기호 사전(아내 요청으로 계속 추가 예정) + 사슬/기둥사슬/이음/매직링 그리기
- `parser.js` 텍스트 → 요소 목록. 요소 kind: st / chain / standing / join. `sg` = 같은 공간 그룹 번호
- `layout.js` 요소 → 좌표. 원형·사각은 `layoutRing(p, shape)` 공유(둘레 위치 f 0~1 → 투영). 앵커(전 단 코/공간) 기반 배치, 같은 앵커 묶음은 부채꼴
- `app.js` 화면·편집·저장·PDF. 저장은 localStorage(`crochet-chart-v1`), 예시 작품은 `SEED_VERSION`으로 기존 사용자에게도 추가

## 진행 상황
- 1차(2026-09-16): 기본 앱 배포
- 2차(2026-09-16): 위 사양 1~8 반영, 배포 완료.
- 3차(2026-09-16): 아내 2차 피드백("기호가 단 높이에 비해 커서 찌그러져 보임, 배열이 삐뚤어 확인 어려움") 반영 — 기호 가로폭 축소(T 가로선 12→8), 코 간격 14→11, 단 높이는 단의 가장 큰 기호 기준 22~36 가변, 사각은 각도 대신 둘레 길이 기준 배치 + 변 위 기호는 변에 수직, 원형은 둘레에 고르게, 단 번호는 왼쪽 위 목록. 배포 완료. 3차 피드백 대기 중.
- 4차(2026-09-19): 패터너스(patterners.com, 손으로 배치하는 뜨개 도안 에디터) 참고해 그림 품질 조정 — 선 굵기 1.4→1.1, 사슬 타원 축소(겹침 제거), 묶음 간격 FAN 7.5→9.5, 단 높이 26~40(기둥이 길어져 묶음이 덜 벌어짐), 안내선을 옅은 점선으로. 텍스트 입력 방식은 유지(패터너스는 기호를 놓고 ＋핸들을 끌어 원형/직선 반복 배치하는 방식 — 편집 모드에 넣을 후보 B안).
- 5차(2026-09-19): 아내 용도 확인(즉흥 패턴 기록, 아이패드 미니, 키보드 없음) → **코 키패드**(기호 크게+이름 작게, 자주 쓰는 6개 1열, 숫자·괄호·반복·다음 단·지움·되돌리기) + 세로/가로 레이아웃 + `공간에 (…)*N` 파서 수정.
- 6차(2026-09-19): 아내 피드백 "한길긴뜨기 묶음 높낮이가 고르지 않다" + 사진 도안 2장 → 기호 머리를 단 선에 맞추고 기둥만 모이게, 기둥사슬을 기둥 길이로, 사슬은 머리와 같은 선. 사진 2의 퍼프뜨기 기호·원형→사각 전환은 미구현(별도 후보).
- 8차(2026-09-19): 아내 카드 사진(짧은/긴/한길긴뜨기 × 2·3코 × 늘려/모아뜨기 12장) 기준으로 `drawFan` 재작성 — 늘림은 아래 한 점에서 갈라져 머리가 같은 높이, 줄임은 위 한 점(A 모양)에서 만남, 다리마다 가로선·빗금, 짧은뜨기는 작은 ×. 입력은 글("짧은뜨기 2코 늘려뜨기")이 원칙이고 키패드의 늘려·모아뜨기 버튼은 **낱말만** 적어 주는 글자 버튼(아내 결정 A안).
- 7차(2026-09-19): 아내 사진(3단 그래니, 보라·파랑·갈색) 기준 "기호가 몰리지 않고 단에 퍼지게" → `spreadHeads`로 머리를 둘레에 균등 분배. 6차와 함께 배포.
- 다음 후보: 퍼프뜨기 기호, 원형 N단 뒤 사각으로 전환하는 규격, 아내 키패드 사용 피드백 반영, 전 단 복사 버튼, 애플펜슬 메모 레이어/사진 첨부, 음성 입력 실험, B안(편집 모드에서 기호 끌어 복제), 타원/나선/육각 규격, 기호 추가(아내 요청 시), 여러 기호 한꺼번에 편집, 단 전체 회전, 라벨 위치 조정, 완성 사진 표지

## 작업 방식
- 피드백을 받으면 먼저 "이렇게 이해했다"를 정리해 남편에게 보여주고(아내에게 확인받을 수 있는 문체), 확인 후 구현.
- 그래니 스퀘어 예시로 시각 확인 후 배포. 로컬 서버 띄우고 브라우저로 `window.CROCHET_APP.exportSvgString(1)` 출력 확인이 빠름.
